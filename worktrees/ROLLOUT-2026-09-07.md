# Worktrees rollout — 2026-09-07

Scope: GitHub repositories in the `maskservice` organization found in the local workspace. Runtime mirrors and unrelated organizations were not changed. Continuation added the necessary verifier repair in subactor/onedev-agent PR #178, independently approved and merged.

## Published on main

These commits add the Worktrees v5 agent policy and exact root ignore rules for worktrees and operational state. They preserve explicit main-only workflows. This is placement-policy adoption, not a claim of full new-project package installation.

| Repository | Commit |
|---|---|
| .github | [75d90f15d8](https://github.com/maskservice/.github/commit/75d90f15d85cabba3a6e6ca80a8a82134cd0c024) |
| c2004 | [86c2b6b1bc](https://github.com/maskservice/c2004/commit/86c2b6b1bc10cd4dbd4538d99ca2d3baf1624064) |
| c2004-firmware | [85e405824b](https://github.com/maskservice/c2004-firmware/commit/85e405824b76bc9611a1b900c71e38259fd95cf3) |
| core | [cdf65d6392](https://github.com/maskservice/core/commit/cdf65d6392bdb403e6c8128c604e9f07da6100a8) |
| deploy | [30ac9a2a75](https://github.com/maskservice/deploy/commit/30ac9a2a7526a2af4f0393e916807a536e3dbafa) |
| firmware | [22eadc2103](https://github.com/maskservice/firmware/commit/22eadc2103fd6b60720ccf179a6b62e8ba70b022) |
| fleet | [9f5f736d08](https://github.com/maskservice/fleet/commit/9f5f736d08930f6730e5d2bafe8eb999567532f0) |
| maskauth | [ce05eb198e](https://github.com/maskservice/maskauth/commit/ce05eb198e959a673576484bf077fb2d5b4b2284) |
| rp2040-keyboard | [6cd2b79e6a](https://github.com/maskservice/rp2040-keyboard/commit/6cd2b79e6add0083046bcfdce7488ad34eb044b6) |
| stacknet | [cf4098faff](https://github.com/maskservice/stacknet/commit/cf4098faffbcd3071f84d528f74948d9d58d1e13) |
| workshop | [bdb7b49875](https://github.com/maskservice/workshop/commit/bdb7b49875f043e76fa626b7e4aeeef1f87bc56e) |

## Managed adoption and Viewer pull requests

The four managed adoption PRs are merged after independent Validator approval. Viewer PR #9 is undergoing fresh-head verification. The managed repositories adopt published `wellmanifest/new-project` 0.20.9, revision `6f302b389b03515e62d4e3ee9d88beb63a89bdcc`, through Goal. Their package contains Worktrees v5.

| Repository | Pull request | Integration |
|---|---|---|
| displaynet | [#1](https://github.com/maskservice/displaynet/pull/1) | `0a2100cfd73e82401531caae770c299ecba0e080` |
| boardnet-digital-twin | [#1](https://github.com/maskservice/boardnet-digital-twin/pull/1) | `2856f89d22bebf28c73ecb8d590e6197c9393366` |
| maskservice-digital-twin-lab | [#1](https://github.com/maskservice/maskservice-digital-twin-lab/pull/1) | `c716ddaa702008cf364b43fa2372a0bdfdf067b5` |
| stacknet-digital-twin | [#1](https://github.com/maskservice/stacknet-digital-twin/pull/1) | `1b8659b938b07995440076999e85473015f49639` |
| viewer | [#9](https://github.com/maskservice/viewer/pull/9) | Pending fresh-head validation |

Viewer now defaults portability-test data to a unique `.subactor/cache` directory. Explicit `PORTABILITY_ROOT` overrides remain supported. The existing print feature branch was preserved.

## Verification

- Shared read-only auditor: six integration tests passed using real Git repositories, including relative/absolute links, branch mismatch, lease absence and overly broad ignore rules.
- Each managed adoption passed the governance gate and `make verify`. BoardNet and StackNet also passed three product tests each, with the new governance pytest plugin enabled.
- BoardNet/StackNet CI now fetches full history and chooses the target base for feature pushes, PR base for PRs and previous main SHA for main pushes.
- Viewer: full run gave 670 passed, 14 skipped and two failures caused by missing CairoSVG. After installing the dependency in local ignored test storage, both failures passed on rerun. Shell syntax validation passed.
- Exact published branch HEADs were compared with GitHub refs.

## Outstanding work

The original external verifier blocker was repaired by [onedev-agent PR #178](https://github.com/subactor/onedev-agent/pull/178), merged as `14323eee99555dc289fdd475a81c72e137a8b01b` after independent Validator approval. Four trusted profiles now run product checks, exact-base governance and the protected branch-lifecycle snapshot. The executor includes the exact twin-projects pin required by Viewer. All five target PRs have since received successful `onedev/local-verify` results. No check was forged, waived or replaced by a self-approval.

The required `delete_branch_on_merge` setting was enabled for the four managed repositories and failed remote-lifecycle jobs were rerun. Their merged delivery branches and owned local worktrees have been released; local integration and lease receipts are retained.

`update` was being changed by other sessions throughout this rollout. Its main already adopted newer governance; existing and newly appearing noncanonical registrations, branch/layout mismatches and missing leases remain separate unresolved observations. Dirty work and open-PR branches were preserved. No lease was fabricated for another session.

The three stale `/tmp/viewer-*` registrations disappeared during concurrent work before this rollout attempted any cleanup. This rollout did not remove them.

A local re-audit observed zero implemented-check findings for the 11 direct-main repositories. The four managed primary checkouts now contain the adopted rules. Viewer retains the user’s existing print feature checkout; its policy is delivered separately through PR #9. Runtime leases and integration receipts remain private local state.

## Audit limits

The auditor inspects registered worktrees, checks branch/path agreement and observes lease-file existence. It does not prove lease validity, process quiescence, ignored-data safety or merge eligibility. There is no automatic migration/prune. Counts are observations of a concurrently changing workspace, not a fleet-wide compliance certificate. Raw inventories and receipts remain in ignored local storage.

## Enforcement follow-up (same day)

A recursive host audit found 39 Git repositories, including nested dependencies.
Only two initially had an executable pre-commit; the configured `.githooks`
path alone had concealed missing managed hooks. The shared read-only checker
was updated to Worktrees 0.5.1 at `81e0d750f18ecace4436706250bf5deb190a000a`.

The final host configuration has executable pre-commit hooks in all 39
observed Maskservice clones: 34 use the additional offline placement guard and
five retain their official managed hook. C2004's existing pre-commit framework
was installed before composition. Original hook files and configuration
receipts are preserved. A compatibility follow-up restored the exact official
hooksPath for managed clones; their agent-host validator rejects wrappers at
another path even if they forward the original hook correctly.

The dependency audit covered 46 local Wellmanifest repositories. After the
same compatibility handoff, 33 have executable hooks; 13 managed clones still
lack their declared original pre-commit. These are unresolved adoption gaps,
not successful installations. Sixteen have no managed package pin and 26 have
no recognized governance CI command. No source adoption was fabricated to
hide these findings.

The 29 behavioral tests exercise real Git commits and configuration: forced
operational-data staging and noncanonical writers are rejected; valid primary
commits and source files under `worktrees/` remain allowed; existing rejecting
hooks remain effective; managed hooksPath stays compatible; and later adoption
restores the original managed hook location. This does not certify product
regression coverage.

Remaining independent findings: 34 Maskservice repositories have no full
managed new-project lock or recognized governance CI invocation. Five managed
repositories have a recognized invocation; the audit accepts both shell and
Python entrypoints. Workflow source is not proof of successful or mandatory CI.
GitHub API observations for `update/main` and `c2004/main` returned neither
classic required status-check protection nor applicable branch rules. Explicit
main-only instructions remain unchanged; this rollout does not silently
replace that workflow with mandatory PRs.

C2004 commit `1b6f8612e` moves the two scenario/template deployment scripts'
working files from `/tmp` to unique repository-local `.subactor/cache`
directories. A shell integration test checks paths containing spaces, scoped
cleanup and preservation of unrelated files. Other dependency temporary-file
uses still need owning-repository changes; a workspace-wide migration is not
claimed.

The LAN menu regression was caused by deployment of unmerged ticket source:
`0e90af8` contained the two-column layout; later main deployment `be2e377` did
not. Pre-commit layout enforcement cannot substitute for protected source
integration, feature tests, or deployment ancestry and readback checks. Open
panel PRs #90 and #111 had merge conflicts at observation time.

Raw observations, local hook snapshots and preservation receipts remain in
ignored repository/Git-local storage. No historical worktree was removed,
moved or pruned by this enforcement follow-up.


## Verifier activation evidence

The verified operational image is `sha256:a2c8efc02c03bde0a5208b51cbd089f11cf2d1fc87dfd3a51660821ac5cf5794`, derived from the then-running executor `sha256:838f9648267f31ca78555386beb36050734e49cecaee4d362ef407216184d44a` by adding only the pinned twin-projects package. A complete source rebuild was not claimed: unrelated local build contexts were missing. The source Dockerfile includes the same pin for future full builds.

Only the verifier services were recreated. Their persistent local Compose override selects this image; the executor still has network mode `none`, no secrets and no host-repository mount. Import probes passed without networking. Private rollback and activation receipts remain in repository-local recovery storage.

The OneDev repair passed 27 configuration tests, pinned Ruff and all four gates of its existing isolated PR verification profile. Full host pytest collection was not used as evidence because the host's global SubLLM reader did not match the pinned configuration. The independent Validator approved the exact repair head before activation.

Viewer local delivery metadata was normalized to ticket-009 for the dispatcher's ticket-NNN interface. The original remote branch name remains an alias until PR #9 is integrated; its tested commit SHA did not change. The local worktree and lease were moved together, preserving all files.

Viewer continuation: the Validator rejected the old head because its required-check history included the pre-repair OneDev failure. Its policy requires a new head rather than erasing earlier failed evidence. Commit `c0331c9` is an empty verification commit with identical files, published for fresh hosted and isolated checks. Before committing, the owned worktree’s two Git pointers were repaired back to relative links after the placement hook detected absolute pointers.
